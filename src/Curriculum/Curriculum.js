let DatabaseCurriculum = function (id, database) {
    this.curriculumId = id;
    this.database = database;
};

let ElementCurriculum = function(element, inner) {
    this.element = element;
    this.inner = inner;
}